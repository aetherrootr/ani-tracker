[project]
name = "ani-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "sqlalchemy>=2.0.51",
]

[dependency-groups]
dev = [
    "mypy>=1.14.0",
    "ruff>=0.15.20",
]
